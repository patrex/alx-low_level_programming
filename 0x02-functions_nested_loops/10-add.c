#include "main.h"
/**
 * add - determines if an input char is lowercase, uppercase
 * @m: the number under test
 * @n: second number
 * Return: 1 if positive. 0 if zero, -1 if negative
 */
int add(int m, int n)
{
	return (m + n);
}
